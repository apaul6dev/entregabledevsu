antes de ejecutar el docker-compose.yml general con "docker compose up --build" debe compilar el proyecto de springboot con "./gradlew build -x test"

Después de ejecutar el Docker compose general, ingresar a:

http://localhost:90

Repositorios:
https://github.com/apaul6dev/frontdevsu.git
https://github.com/apaul6dev/backdevsu.git